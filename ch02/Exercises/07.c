/* are keywords: a(for) e(while)
 *b(If) is not because the I is capital
 *c(main) afaik is not a reserved keyword but it should not be used for any other case besides main function
 *d(printf) is not a keyword, but it is a function in the standard library
*/
