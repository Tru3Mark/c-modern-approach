#include <stdio.h>

int main(void)
  {
    printf("hello world\n");
  }

// I do not get a warning, i think the warning would've been because no return statement, but now it is not a required feature
